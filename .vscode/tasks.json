{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build NUT",
            "type": "shell",
            "command": "docker buildx build --rm --platform linux/arm/v7,linux/amd64 -t paolooranges/nut-upsd:1.0 -t paolooranges/nut-upsd:latest --push --file ./NUT/Dockerfile ./NUT"
        }
    ]
}